<h3>Edit comment</h3>
<div>
  <%= form_for(@comment, url: post_comment_path(params[:post_id])) do |f| %>
    <p>
      <%= f.text_area :body, class: 'form-control', rows: 2  %>
    </p>
    <p>
      <%= f.submit 'Submit', class: 'btn btn-primary' %>
    </p>
    <br>
<% end %>
</div>