<h3>Edit comment</h3>

<%= form_for(@comment, url: admin_post_comments_path(params[:post_id])) do |f| %>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
  <br>
<% end %>